# 更新日志

## [1.0.5](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.5) - 2026-09-19

- 修复长灵感对话超过上限后，“整理设定”也被同一上限拦截的问题；按时间顺序分批整理，显示进度并保留全文，后续对话使用整理后的上下文。
- 最近 5 个未成书灵感会话迁移至项目数据库并纳入云同步及项目备份，包含聊天、未发送草稿、搜索开关、参考知识库选择及已整理上下文；按会话处理版本冲突。
- 注册与恢复密码改为至少 6 个字符，增加重复密码确认；已登录用户可使用当前密码修改密码，成功后撤销全部设备登录。
- 内置写作技能支持查看、修改、保存及恢复默认规则；“向量记忆”更名为可选的“语义检索增强”，不影响结构化记忆和数值计算。
- 普通云同步界面不再显示或编辑服务器地址，保留内部默认地址与已有账号绑定，不改变作品归属。
- 普通 PDF 视觉识别默认关闭，设置页与导入页可明确开启并保存选择；关闭时保留文字层读取与已有缓存，无文字页面停止并提示本地 OCR，不自动发送图片。独立的疑点视觉复核仍需另行授权。
- PDF 增加只读已识别缓存、逐页预览、有效文字页 / 空白结果 / 缺失缓存统计、预计导入条数和文本导出；疑似不完整结果必须另外确认。
- 增加本地 OCR＋疑点视觉复核入口：按页码范围离线识别，保留置信度、坐标和原始结果；对可疑区域明确授权后按请求上限分组复核，取消、暂停及刷新后续读；数字改动与表格歧义保留待确认，可导出复核记录。当前本地初读仍有明显误字，不宣传免校对准确率，详见 `docs/ocr-import.md`。
- 修复 PDF 中文 CMap、标准字体及 JBIG2 / JPEG 2000 解码资源未随应用加载的问题，避免文字层被误判为空、扫描正文渲染成白图；图像解码失败时停止，不发送失败页面给 AI。旧版空白视觉缓存会重新检查，非空缓存继续保留。
- 使用用户提供的 857 页原 PDF 在隔离环境验证：默认自动模式读取约 90 万字符、导入 857 条并在刷新后保留，本机约 19 秒，AI 请求为零；模拟旧版 856 页空白缓存的恢复也通过。原 PDF 自带的文字层仍有识别误字，读取成功不等于资料已核实或按主题整理。
- 自动更新检查失败后按 1、5、15、30、60 分钟退避重试；联网恢复、窗口激活或休眠恢复时检查过期状态。已发现新版但更新清单不可用时仍提示版本，保持下载安装校验和人工确认。
- 发布 Windows x64 安装包及同次构建的更新清单、分块文件和 SHA-256。同步服务端兼容原有 1.0.4 客户端；未成书会话同步等新功能需两端均升级至 1.0.5。

完整范围、验证结果及质量限制见 [1.0.5 发布说明](docs/releases/1.0.5.md)。

## [1.0.4](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.4) - 2026-09-17

发布日期按 UTC 记录，对应北京时间 2026-09-18 凌晨。

### 可选云同步

- 新增邀请码注册、账号登录、一次性恢复码和管理员邀请入口；不同账号的数据独立。
- 显式开启后同步小说正文与历史版本、剧情规划、角色、故事时间、数据面板、书内对话，以及知识库正文与摘要。
- 本地优先保存，离线后可继续写作，恢复网络后自动比较版本；设置页提供立即同步和状态、空间显示。
- 按小说和知识库分别同步；通过版本比较防止覆盖并发修改，保留冲突的双方内容及未采用版本的本机备份。
- 编辑器打开或 AI 任务运行时延后云端内容合并；增加同步、冲突处理、项目导入及更新安装的并发保护。
- 本机书架绑定同步账号，防止换号误传；模型密钥、原始资料文件、PDF 识别缓存和未成书的创建会话不上传。

### 服务与安全

- 新增自托管 FastAPI / SQLite 服务、非 root 运行配置、HTTPS 反向代理、自动证书续期和每日备份。
- 密码使用 Argon2id 哈希，桌面会话使用操作系统加密，支持退出与恢复密码后撤销登录。
- 加入身份验证限流、账号及总存储配额、文档大小限制和服务端版本回退保护。
- 调整窄窗口导航和冲突信息展示；增加同步状态机、实际数据库回滚、加密会话和两设备验收。

完整使用范围、配额与隐私说明见 [1.0.4 发布说明](docs/releases/1.0.4.md)。

## [1.0.3](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.3) - 2026-09-17

### 知识库与扫描 PDF

- 单个资料文件上限提高到 100 MB，支持文字版、扫描版和混合 PDF。
- 扫描页可使用已配置的视觉模型识别，无须单独配置 OCR 服务；默认优先文字层，也可选择全部页面视觉识别。
- 分页进度、暂停、失败后继续与本机持久缓存；重新选择相同内容的文件和读取方式复用已完成页。
- 拒绝将被截断或格式异常的识别回复当作成功结果；保留每页来源编号，人工预览后确认导入。
- 识别调用沿用 OpenAI 兼容聊天接口的图片消息格式；开始前提示页面图片发送与可能费用。
- 修复短资料、单行事实导入时丢失的问题；导入后可选自动摘要，限制并行数量，避免长文件集中发起大量请求。

### 对话与界面

- 灵感对话增加参考知识库选择，读取最新摘要和相关原文片段；对话引用范围与新书挂载范围分开。
- 灵感对话、书内助手支持将回复保存为知识条目，可选择已有库或新建库，编辑并确认后写入本机。
- 灵感对话、书内助手和写作顾问增加软件身份与实际功能说明；书内助手增加当前编辑章节上下文，仍只读取本书挂载资料。
- 全局组件启用中文语言和日期设置，确认、取消、选择器、设置参数等交互中文化，保留模型与协议原始标识。
- 新增资料读取范围、即时刷新、保存失败重试、PDF 图像消息、分页缓存及界面回归测试。

完整说明与限制见 [1.0.3 发布说明](docs/releases/1.0.3.md)。

## [1.0.2](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.2) 安装包修订 - 2026-09-16

程序版本号保持 `1.0.2`，替换同一发布页中的 Windows 安装包及配套校验文件、更新清单，不创建新版本或移动原标签。已安装旧 `1.0.2` 的用户需手动重新下载并安装，同版本不会触发更新提示。本次安装包的功能源码对应提交 `aed96df8d2ea0cbee9ebef9468ccb0e0f904c60a`。

### 联网诊断与兼容

- 设置页区分“测试对话”和“测试联网”，展示实际选择或自动推测的搜索协议。
- 联网测试需确认后发起，支持停止和复制诊断；不发送小说内容，不自动切换协议或重复请求。
- 区分身份验证、权限、额度、连接、超时、接口格式和搜索工具错误；诊断不包含 API Key 或服务商原始错误内容。
- 兼容部分中转站在非流式 Claude 请求中返回事件流，保留搜索结果与引用，识别不完整响应。
- 只有收到搜索证据和有效来源，联网测试才显示已验证；接口仅声称执行搜索但没有来源时，仍显示未完整验证。
- 编辑模型时测试可使用已保存的密钥，无需重复填写；调整设置页窄窗口布局。

### 范围说明

- 未接入 Exa MCP 或其他独立搜索服务，联网能力仍取决于所选模型接口和服务商是否支持搜索。
- 增加响应解析、诊断、取消、密钥保留及界面回归测试。

## [1.0.2](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.2) - 2026-09-13

### 新增

- Windows 安装版启动自动检查 GitHub 正式版本，设置页提供手动检查和自动检查开关。
- 全局新版提示、更新说明、发布日期和发布页入口。
- 按需下载安装包，显示下载进度，支持取消、失败重试和下载后的安装确认。
- 接入 `electron-updater`，生成 `latest.yml` 自动更新清单；不接收预发布版本或降级更新。

### 安装保护

- 默认不自动下载，也不在普通退出时自动安装，必须由用户确认“安装并重启”。
- 安装前保存编辑器中的未提交正文，再保存小说、知识库和配置；保存失败或超时不执行安装。
- 检查 AI 生成、联网对话、后台任务与项目导入状态，任务未结束时阻止更新安装。
- 安装准备期间禁止启动新的 AI / 后台任务或导入项目，避免保存与退出之间产生新修改。
- 校验发布版本、安装包名称、下载地址和 SHA-512；异常或篡改文件不进入可安装状态。
- 浏览器 / 开发环境明确显示不支持安装更新，不假报“已经是最新版本”。

### 文档与验证

- 更新首页介绍、下载版本、自动更新用法和维护者发布流程。
- 增加版本比较、并发检查、取消下载、保存失败、任务保护、界面及真实下载校验测试。
- 1.0.0 / 1.0.1 用户需先手动安装一次 1.0.2，之后可检查后续新版本。

## [1.0.1](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.1) - 2026-09-11

### 新增

- 故事时钟：记录章节经过的天数与故事当前时间，支持 AI 识别明确时间流逝及人工校正。
- 时间驱动的数据规则与作物模板：记录成长周期、额外进度、成熟度及剩余时间。
- 章节数据提取：主笔与 AI 模式均可提取正文中新出现的装备、道具、数值与归属，带证据进入待确认列表。
- 装备状态与属性映射：区分持有、装备中、已消耗和已失去；支持固定值与基础属性百分比加成。
- 角色有效属性展示：保留基础属性，独立计算已装备物品提供的加成；卸下、转移或修改装备后重新计算。
- 数据面板待确认入口，可在正文侧栏或独立数据面板中接受、拒绝变更。

### 优化

- 已确认的数据、装备后的有效属性纳入写作上下文与语义记忆。
- 防止重复扫描或重复确认导致同一数据重复录入；后台结果关联原小说和章节，正文变更后的过期结果不写入。
- 为新增对象初始化时间规则基线，避免将创建之前的时间计算为成长时间。
- SQLite 持久化、项目备份和数据版本快照覆盖故事时钟与装备结构。

### 修复

- 本机开发环境加载配置时恢复仍可用的会话密钥，减少配置迁移造成的 API Key 丢失。
- 编辑模型时 API Key 留空保留已有密钥，只有输入新密钥才替换。
- “关于”页从应用包版本读取版本号，避免继续显示过期的固定版本。
- Windows 程序文件属性写入应用版本，不再沿用 Electron 运行时版本；打包启动测试校验版本一致性。

### 文档与发布

- 新增中文首页介绍、模式对比、下载与开发说明、数据隐私说明。
- 更新写作辅助的并行分析与显式停止回归测试，修正按钮测试的插槽渲染配置。
- 发布 Windows x64 安装包及 SHA-256 校验文件。
- 完整验证记录与已知限制见 [1.0.1 发布说明](docs/releases/1.0.1.md)。

## [1.0.0](https://github.com/HannJia/novel-ai-studio/releases/tag/v1.0.0) - 2026-09-10

- 首个可下载的 Windows 安装版本。
- 提供写作辅助与 AI 正文模式、灵感对话、剧情规划、章节编辑与审查。
- 包含书架知识库、摘要与分类、按书挂载资料，以及项目备份和正文导出。
