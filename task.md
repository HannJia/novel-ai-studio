# AI Novel Writer 重构记录与开发路线图

> 最后更新：2026-09-02  
> 本文件是当前产品边界、完成状态和后续任务的唯一记录。代码或产品决策变化后必须同步更新。

## 1. 当前产品定义

这是一个本地优先的 AI 长篇小说写作桌面应用。当前只维护界面中可访问、能形成完整写作闭环的功能。

### 写作主流程

1. 创建小说并生成全书大纲。
2. 从全书大纲自动提取分卷规划，进入“剧情规划 / 分卷规划”复核。
3. 确认分卷规划后生成下一章、近期、远期三层章节计划。
4. 进入章节编辑器，生成前确认本章关联数据，AI 再制定本次写作计划并生成正文。
5. 正文达到目标字数后检查单章剧情是否完整，不以字数达标作为截断依据。
6. 完成章节后进行审查、状态提案、数据变化确认和长期记忆更新。

### 剧情规划层级

`全书大纲 → 分卷规划 → 章节计划 → 正文`

- 全书大纲：全书方向、核心冲突和总体结构。
- 分卷规划：每卷目标、剧情概要、关键转折、角色变化和预计篇幅。
- 章节计划：下一章详细计划、近期推进路径、远期方向与收束条件。
- 故事弧线：跨章节目标及里程碑。
- 故事时间线：已发生、待推进的关键事件；`chapterIndex = -1` 表示“全书规划”。
- 项目记忆：章节、角色、事件、故事弧线、章节计划、数据面板和知识库的统一检索。
- 状态提案：AI 只提出状态变化，用户确认后才写入。

### 当前工作台入口

- 章节列表
- 大纲
- 角色库
- 知识库
- 剧情规划（包含分卷规划）
- 数据面板
- 导出小说
- 设置

### 统一术语

| 当前名称 | 说明 | 已废除名称 |
| --- | --- | --- |
| 分卷规划 | 剧情规划中的卷级结构 | 分卷大纲独立页面 |
| 章节计划 / 下一章 | 当前章节的详细计划 | 章节细纲、`Chapter.outline` |
| 全书规划 | 故事时间线中的全书级事件 | 全局伏笔、独立伏笔追踪 |
| 章节总结 | 已完成章节的摘要 | 章节列表中误标的“细纲” |

## 2. 当前技术结构

| 层面 | 实现 |
| --- | --- |
| 桌面运行 | Electron |
| 前端 | Vue 3 + TypeScript + Vite |
| UI | Naive UI |
| 状态管理 | Pinia |
| 数据存储 | sql.js / SQLite，通过 Electron IPC 持久化 |
| AI 接口 | OpenAI 兼容接口，支持流式输出和多任务模型配置 |
| 长期记忆 | 本地语义索引，可选远程 Embedding |
| 测试 | Vitest + vue-tsc + Knip |

核心目录：

```text
app-main/       Electron 主进程与预加载
src/components/ 通用交互组件
src/services/   AI、上下文、数据、导出与数据库服务
src/stores/     Pinia 业务状态
src/types/      当前领域模型
src/views/      当前可访问页面
```

## 3. 已完成能力

### 核心写作闭环

- [x] 小说创建、全书大纲生成与编辑
- [x] 从全书大纲自动添加分卷规划
- [x] 分卷规划并入剧情规划，旧 `/volumes` 地址重定向到对应标签
- [x] 确认分卷规划后生成三层章节计划
- [x] 章节计划生成读取分卷目标，不再把各层规划作为独立孤岛
- [x] 旧章节细纲字段、数据库列写入、上下文接口和 UI 文案清理
- [x] AI 正文生成、流式输出、草稿 Diff 确认和自动保存
- [x] 2000~2500 字软目标、3000 字硬上限和自然句收尾
- [x] 章节结尾完整性检查与自动补完，不因达到字数强行截断
- [x] 内容一致性审查、自动重写、复审和定稿入库

### 剧情规划与长期记忆

- [x] 分卷规划
- [x] 下一章 / 近期 / 远期章节计划
- [x] 故事弧线与里程碑
- [x] 故事时间线与全书规划
- [x] 章节完成后的故事状态提案
- [x] 章节、角色、事件、规划、数据面板和知识库统一语义检索
- [x] 独立伏笔追踪入口和重复界面已删除

### 数据面板 P0：管理体验

- [x] 数据变更支持编辑新值后再应用
- [x] 文本、数字、百分比、天数、倒计时和公式字段
- [x] 角色面板、作物成长、资源库存、建筑进度、任务倒计时模板
- [x] 待确认变更批量应用 / 批量忽略
- [x] 变更历史按章节、对象、状态筛选
- [x] 已处理记录按最近 N 条、最近 N 章或全部清理
- [x] 所有清理策略强制保留待确认记录

### 数据面板 P1：自动计算与写作联动

- [x] 每章 `+N`、每次出现 `+N`、经过 `X` 天后推算规则
- [x] 分阶段百分比变化，例如首章 `+5`、后续 `+4`
- [x] 作物已成长 / 总周期 / 剩余天数公式联动
- [x] 自动推算只生成待确认变更，用户确认后才写入
- [x] 生成章节前按“下一章”计划推荐关联数据并让用户确认
- [x] AI 生成时优先遵守已确认关联数据
- [x] AI 扫描区分明确变化、可能变化和无变化
- [x] 数据变化记录保留正文依据

### 数据面板 P2：全局管理与可视化

- [x] 独立数据面板工作台
- [x] 按分类、关键词、最近出现章节筛选数据对象
- [x] 按对象、字段、状态查看变化时间线
- [x] 导出数据面板为 JSON / Markdown

## 4. 2026-07-23 正式重构记录

- [x] 删除独立伏笔追踪页面、路由和导航；保留剧情规划内全部功能
- [x] “全局伏笔”内部写作语义统一为“全书规划”
- [x] 删除不可达页面、旧 JSON 迁移、增量 Schema 兼容层和重复 Electron 入口
- [x] 删除无界面入口的自定义词库、白名单和平台预设代码
- [x] 清理无调用 Store 方法、服务导出和旧 AI 字段名
- [x] 设置占位页面改为正式设置页面命名
- [x] 创建向导删除未开放知识库占位功能
- [x] 引入 Knip 死代码审计
- [x] 生产依赖安全审计为 0 漏洞
- [x] 分卷规划并入剧情规划，保留原功能与旧地址跳转
- [x] 删除重复的章节细纲模型，统一使用“章节计划 / 下一章”
- [x] 数据面板 P0、P1、P2 当前规划全部落地
- [x] 完成本轮浏览器回归并记录结果

## 5. 2026-07-23 稳定性、模块化与性能阶段

### P0：稳定性与回归

- [x] 为“总纲 → 分卷 → 章节计划 → 正文”增加服务级流程测试
- [x] 为数据面板批量 Store 操作增加 Pinia 集成测试
- [x] 覆盖数据面板窄屏、空状态、大量历史记录和导出回归
- [x] 检查浏览器控制台和 Electron 主进程错误日志

### P1：编辑器模块化

- [x] 将 `ChapterEditor.vue` 中的数据面板、审查、AI 状态拆为独立组件，并将数据选择、结尾检查、AI 生成拆为组合式函数
- [x] 数据面板编辑器改为结构化字段与规则控件，保留文本批量编辑作为高级模式
- [x] 分卷规划与章节计划增加版本对比和局部重新生成
- [x] 章节完成后自动完成当前计划并补齐下一章计划，避免旧计划占位和滚动计划断档

### P2：性能与依赖升级

- [x] 知识库 Word、EPUB、PDF 解析器改为按需动态导入，知识库主脚本由约 867 kB 降至约 13 kB
- [x] Vite 升级到 8.1.5、Electron 升级到 43.2.0，并完成开发依赖安全审计
- [x] 完成 300 章语义索引、10000 条历史筛选和 5000 条 SQLite 记录保存 / 重载性能回归

### 真实小说全流程回归

- [x] 创建科幻小说《星标遗痕》，测试书 ID：`mrwhi3m0ggqmjx`
- [x] 生成总纲并自动衔接 4 卷分卷规划
- [x] 验证分卷版本对比、局部重新生成和版本快照累积
- [x] 确认分卷后生成 1 条下一章、3 条近期、3 条远期计划
- [x] 验证章节计划版本对比和局部重新生成
- [x] 创建结构化数据对象 `BX-247 航标`，包含百分比、数字字段和“每章 -1%”规则
- [x] 第一章生成前自动推荐并确认关联数据
- [x] 生成 2559 字第一章，结尾检查确认剧情节拍完整，未因达到目标字数强行截断
- [x] 正文通过一致性审查、违禁词审查和章节总结
- [x] 数据扫描仅生成待确认变更；批量忽略重复识别，批量应用自动规则后能量完整度由 77% 变为 76%
- [x] 完成第一章后自动补齐第 2 章计划，并修复旧第 1 章计划仍占据“下一章”的状态同步问题
- [x] JSON / Markdown 导出均显示成功；修复导出菜单仅悬停触发、触屏点击无法展开的问题
- [x] 390 × 844 窄屏下数据面板、空状态和结构化编辑弹窗无水平溢出
- [x] 浏览器控制台未产生新的应用错误；旧 Vue Router 命名警告已修复
- [x] Electron 43.2.0 主进程和渲染进程持续运行约 50 秒，进程响应正常且无错误输出

## 6. 2026-07-23 正式使用可靠性阶段

### P0：关闭保存与恢复

- [x] Electron 关闭窗口前等待小说、知识库和模型配置全部写入完成
- [x] 保存失败或超时时阻止直接退出，并允许用户取消退出后重试
- [x] SQLite 写入前验证文件头，覆盖前保留 `novel-writer.db.bak`
- [x] 主数据库损坏时自动读取最近有效备份
- [x] 修复内存事务已经提交后，磁盘保存失败仍执行 `ROLLBACK` 的错误路径
- [x] 模型配置与知识库保存队列串行化，关闭时写入最新快照
- [x] Windows 标准关闭回归通过：主窗口完成保存握手后退出，主数据库与 `.bak` 同步更新且无临时文件残留

### P0：后台任务目标一致性

- [x] 后台任务记录创建完成后再启动任务，避免短任务永久停留在待处理状态
- [x] 章节完成后的数据扫描、违禁词审查、时间线和故事状态任务固定小说与章节目标
- [x] 批量生成和批量审校在入队时固定小说 ID，切换书籍不会中断剩余任务
- [x] 后台审查完成时重新核对当前路由，不把旧章节结果显示到新章节

### P0：数据面板幂等与冲突保护

- [x] 同一章节、对象和字段的变化在所有处理状态下保持幂等，忽略后重复扫描不会重新出现
- [x] 批量应用跨章节变化时按章节和创建时间正序执行
- [x] 应用前校验当前值与建议旧值，冲突记录保留待确认且不覆盖现有数据
- [x] AI 扫描跳过空值、无变化值和重复建议，提示数量只统计真正新增记录
- [x] 增加任务持久化顺序、数据幂等、顺序应用和冲突保护回归测试
- [x] Knip 审计入口关闭高虚拟内存占用的实验性 raw transfer，低内存 Windows 环境可稳定运行

## 7. 质量门槛

每个阶段至少执行：

```bash
npm run typecheck
npm test
npm run audit:dead-code
npx vite build
npm audit --omit=dev --audit-level=high
```

当前结果：

- `typecheck`：通过
- `test`：31 个测试文件，86 项测试通过
- `audit:dead-code`：通过
- `vite build`：Vite 8.1.5 构建通过，无大分块或 CJS 警告
- `npm audit --omit=dev --audit-level=high`：通过，生产依赖 0 个漏洞；`nanoid@3.3.16` 与 `postcss@8.5.22` 已分别升级到 `3.3.18` 与 `8.5.26`
- 浏览器回归：通过
- Electron 源码模式与打包版真实启动回归：通过
- 10000 条历史筛选：约 8 ms
- 5000 条 SQLite 记录保存与重载：约 292 ms

已知非阻断项：

- PDF worker 约 2.16 MB，属于按需加载的独立 worker，不进入知识库页面主分块。
- 全量依赖审计已在 2026-09-02 收尾阶段完成升级与定向覆盖，当前 `npm audit` 为 0 个漏洞。
- 浏览器自动化运行器自身的远程遥测连接告警不来自本项目；应用页未出现新的 error / warn。

## 8. 2026-07-23 P1 高收益优化与 P2 大书稿、安全阶段

### P1：AI 写作工作流

- [x] 新增快速、均衡、严谨三档工作流；所有模式均保留章节结尾完整性门禁
- [x] 写作计划、正文流、自检、结尾检查和结尾补写共用同一取消信号，停止后不再继续后续 AI 请求或自动重试
- [x] 快速模式减少写作计划、自检和扩展后台检查；均衡模式作为默认；严谨模式提高结尾补写与审查轮次
- [x] 章节总结与命名改为非流式结构化结果，增加 JSON 校验、自动重试、占位标题识别和标题规范化
- [x] 将正文取样、章节结构分析、角色补录和数据面板变化扫描移出 `ChapterEditor.vue`，统一到 `useChapterAnalysis`

### P2：大书稿性能

- [x] 语义索引由全量删除重建改为增量同步
- [x] 正文或标题未变化时复用已有向量；仅业务元数据变化时只更新记录，不重复调用 Embedding
- [x] 自动清理已删除章节、事件、规划、数据对象或知识条目的索引记录
- [x] 索引同步界面展示更新、移除和新增向量数量
- [x] 500 章增量回归通过：第二次同步生成 0 条向量，单章摘要修改只重算 1 条

### P2：密钥与 Electron 安全

- [x] Electron 使用 `safeStorage` 加密 API Key；系统加密不可用时拒绝明文落盘并降级为仅当前会话保存
- [x] 浏览器模式的 API Key 从永久 `localStorage` 迁移到 `sessionStorage`，持久化配置只保留脱敏字段
- [x] 设置页显示当前密钥存储状态，明确“系统加密”或“仅当前会话”
- [x] 主进程校验 IPC 调用方、配置 JSON、配置大小、数据库大小和密钥密文格式
- [x] Electron renderer 启用 sandbox、context isolation 和精确导航来源校验，外链仅允许 HTTP(S)

### 打包与回归

- [x] 增加 `release/` 独立打包目录、ASAR、Windows NSIS 配置和离线目录打包入口
- [x] 增加 Vue 组件测试，覆盖 AI 停止交互、数据面板空状态和 150 条历史记录渲染上限
- [x] 增加 Electron 冒烟测试，使用临时用户目录验证页面渲染、预加载桥接、Node 隔离和安全存储状态
- [x] 源码 Electron 与 `release/win-unpacked` 打包版均通过隔离冒烟测试
- [x] 离线目录打包通过；正式安装器仍需在可访问构建依赖下载源且配置签名证书的发布环境中生成

## 9. 2026-08-28 人工主笔辅助能力第一版

产品方向：AI 继续负责总纲、分卷、章节计划和故事线规划，正文由作者亲自完成；AI 在正文创作阶段提供有依据的后续思路和规划建议，不自动改写或写入正文。

- [x] 新增 `writingAdvisor` 服务，统一构建当前段落 / 当前场景 / 本章走向的辅助请求
- [x] 新增结构化 `WritingAdviceResult`，每个方向包含剧情思路、故事作用、下一拍、风险和证据来源
- [x] 新增下一章计划草案输出，保持“候选建议 → 作者确认”的边界，不自动写入章节计划
- [x] 新增写作辅助侧栏，支持分析范围切换、方向选择、思路复制、停止、重新分析和清空
- [x] AI 提示词明确禁止代写正文、改写正文和编造未提供的设定
- [x] 建议请求使用当前小说 / 章节 / 正文签名快照，正文变化后结果标记过期并拒绝作为当前建议展示
- [x] 复用现有上下文预算、章节计划、故事时间线、角色、数据面板、知识库和语义索引
- [x] 桌面与 390 × 844 窄屏布局回归通过；窄屏侧栏覆盖工作区且无水平溢出
- [x] 新增写作辅助服务与组件测试；全量测试 28 个文件、75 项通过
- [x] 类型检查、Vite 构建和 Knip 死代码审计通过

待后续迭代：增加本地规则优先的轻量连续性提醒，并支持作者确认后把建议转为章节计划或场景笔记。

## 10. 2026-08-29 正文创作方式切换

- [x] 新增可持久化的“正文创作方式”：默认人工主笔辅助，可切换为 AI 生成正文
- [x] 设置页增加“人工主笔辅助 / AI 生成正文”单选切换，并明确两种方式都保留规划、审查、数据面板和写作辅助（该全局入口已在第 24 节移除，改为仅按书选择）
- [x] 章节编辑器顶部增加当前模式按钮，可直接切换并显示即时说明
- [x] 人工主笔模式隐藏 AI 正文生成按钮，屏蔽 `Ctrl+Enter` 续写和新章节自动续写；写作辅助侧栏继续可用
- [x] AI 生成入口和关联数据确认后的后续执行增加模式校验，防止切换模式后误发正文生成请求
- [x] 增加配置持久化回归测试；类型检查、全量测试、Vite 构建和死代码审计通过

## 11. 2026-08-29 写作辅助常驻布局

- [x] 移除编辑器工具栏中与侧栏重复的写作辅助灯泡入口，避免两个入口视觉重叠
- [x] 人工主笔辅助模式自动常驻写作辅助侧栏，并隐藏关闭按钮；AI 正文模式保留手动开关
- [x] 侧栏打开时桌面端工具栏自动让位，窄屏侧栏全宽展示且隐藏底层工具栏
- [x] 数据面板 / 审查面板的固定切换按钮同步让位，避免与编辑器工具按钮重叠拦截点击
- [x] 展开的数据面板 / 审查面板覆盖写作辅助并置于最上层，关闭后恢复写作辅助
- [x] 窄屏侧栏下移到顶部控制区之后，保留正文创作方式切换入口
- [x] 增加常驻 / 关闭 / 唯一入口组件测试，并完成桌面与 390 × 844 浏览器回归

## 12. 2026-08-29 真人流程模拟

- [x] 从书架进入创建向导，验证类型 / 子类、目标字数、写作风格选择和下一步校验
- [x] 填写主角、世界观、力量体系、核心冲突和其他设定，验证上一步 / 下一步返回后内容保留
- [x] 设置页验证正文创作方式在人工主笔辅助 / AI 生成正文之间切换并正确持久化到界面
- [x] 添加模型对话框验证必填字段校验；未提交模型获取请求，避免测试时发送真实 API Key
- [x] 检查浏览器控制台，测试期间无错误
- [x] 使用真实模型完成大纲、规划、章节和正文生成的端到端流程（详见第 15 节）

## 13. 2026-08-29 模型发现编辑保存修复

- [x] 修复编辑模型时全选多个已发现模型导致底部“保存”按钮消失的问题
- [x] 编辑模式同时保留“保存当前配置”和“批量添加”，分别对应单个更新与多模型配置创建
- [x] 浏览器回归“3 个模型全选 → 保存 → 模型列表回显”流程

## 14. 2026-08-29 窗口大小适配修复

- [x] 修复主题提供器内部根节点未参与纵向布局，导致小窗口页面内容被 `#app` 裁断的问题
- [x] 为应用提供器、应用 shell 和主内容区补充固定高度、可收缩高度与 `min-height: 0` 约束
- [x] 设置页在 639 × 698、390 × 844 和 1280 × 800 窗口下均可滚动到底部
- [x] 窄屏与桌面窗口均确认无横向溢出，浏览器控制台无新增错误

## 15. 2026-08-29 真实模型拟人全流程回归

使用已配置的 OpenAI 兼容中转模型，在独立测试小说《星图残响》（小说 ID：`mte741rq09xhxl`）中模拟作者从规划到定稿的完整操作。测试数据均为本轮生成的虚构内容，不触碰现有书稿。

- [x] 创建测试小说并完成类型、目标字数、风格、主角和世界观设定填写
- [x] 真实生成全书总纲，自动提取 5 卷分卷规划；页面正确显示各卷章节数（约 67 / 83 / 94 / 89 / 78 章）和总字数（约 93.6 万字）
- [x] 在剧情规划中确认分卷规划，生成下一章、近期、远期三层章节计划；故事弧线、事件、项目记忆和全书时间线均可见
- [x] 角色库自动导入 7 个角色，并验证手动新增角色与角色关系图
- [x] 创建知识库和“克尔维航站”条目，确认测试小说绑定关系
- [x] 创建数据对象 `MK-IV 导航仪状态`，添加百分比字段和“每章 -5%”自动规则，验证编辑值、历史记录和规则联动
- [x] 切换到人工主笔辅助模式；写作辅助在当前场景范围返回多个后续方向、故事作用、下一拍、风险、依据和下一章计划草案，确认不会修改正文
- [x] 手动输入第一章正文并验证自动保存；正文达到 2,000 字门槛后才允许完成章节
- [x] 章节结尾检查通过，确认结尾建立在已完成行动结果上，而不是达到字数后强行截断
- [x] 内容一致性审查识别出第一版正文的重复内容和跨章提前信息；修订后复审为“可通过”，必改问题为 0
- [x] 违禁词审查显示未发现明显违规内容；章节总结和标题状态正常生成
- [x] 完成本章后章节状态变为“已完成”，并在定稿入库后变为可生成下一章；章节列表同步显示“已定稿”
- [x] 在审查面板展开状态下仍可点击“生成下一章”；成功创建第 2 章并与下一章计划衔接，章节列表显示 2 章
- [x] 自动数据扫描生成 1 条待确认变化（50 → 45）；验证编辑新值、批量应用、幂等历史和应用后 45% 持久化
- [x] 定稿入库完成故事状态提案、故事时间线更新和语义记忆同步；后台队列最终清空，控制台无错误或警告
- [x] 小说导出 TXT、Word（`.doc`）和 EPUB 均显示成功；数据面板 JSON / Markdown 导出均显示成功
- [x] 重新加载编辑器和数据面板工作台，正文、定稿状态、审查报告、数据值和历史记录全部保留
- [x] 浏览器窄屏、侧栏覆盖、数据面板与审查面板切换回归通过；未发现横向溢出或按钮遮挡
- [x] 修复审查 / 数据面板从窗口顶端覆盖编辑器顶部操作栏的问题；展开面板从顶部控制区下方开始，仍可覆盖写作辅助

## 16. 2026-09-02 传递依赖安全修复

- [x] 通过 npm `overrides` 将 `nanoid` 固定到 `3.3.18`、`postcss` 固定到 `8.5.26`
- [x] 更新 `package-lock.json`，并通过 `npm ci --dry-run` 锁文件一致性校验
- [x] `npm audit --omit=dev`：0 个漏洞
- [x] 完成单元测试、类型检查、渲染器生产构建和 Electron 打包版冒烟测试
- [x] 全量开发依赖审计告警已处理；Electron Builder、PDF.js 及其传递依赖升级后 `npm audit` 为 0 个漏洞

## 17. 2026-09-02 功能收尾与 Windows 发布包

- [x] 写作辅助建议支持作者确认后转为下一章章节计划，或保存为当前章节场景笔记；不自动写入正文
- [x] 增加本地规则优先的实时连续性提醒：数据数值、角色状态、重复段落检查，不调用 API、不修改书稿
- [x] 正文、分卷规划、章节计划和数据面板支持历史版本快照与恢复，单对象最多保留 20 条编辑前版本
- [x] 项目备份支持小说、章节、规划、数据面板、版本记录和知识库的 JSON 导出 / 导入；导入覆盖前必须二次确认，API Key 不进入备份
- [x] 删除小说改为移入回收站，支持恢复和二次确认后的永久删除；数据库仅在永久删除时物理清理
- [x] 书架增加项目备份、项目导入和回收站界面，知识库 Store 增加全量替换导入能力
- [x] 新增连续性扫描和项目备份格式测试；全量测试 31 个文件、86 项通过
- [x] 升级 `electron-builder` 到 26.15.3、`pdfjs-dist` 到 6.3.289，并定向覆盖 `brace-expansion` / `undici` 修复版本；`npm audit` 与生产依赖审计均为 0 漏洞
- [x] 渲染器构建和 `release/win-unpacked` 目录打包通过；打包版 Electron 冒烟测试通过
- [x] 已生成 Windows x64 便携版压缩包，解压后可直接运行 `AI Novel Writer.exe`
- [ ] NSIS 安装器因构建环境下载 GitHub NSIS 工具超时未生成；具备网络访问时可重新运行 `npm run build` 生成安装器

## 18. 2026-09-05 灵感创建、可编辑大纲与辅助标签固定

- [x] 创建小说先展示四个入口：AI 模式、写作辅助、灵感模式、自选设定；原有设定表单和规划能力保留（现已按第 22 节改为两组独立搭配）
- [x] 灵感模式支持流式聊天、停止、失败重试和“整理设定并检查”；AI 整理题材、主角、世界观、风格与篇幅后，作者可修改表单，确认后才创建小说
- [x] 保留本次灵感聊天，检查设定后可返回继续对话；创建成功后保存对话记录
- [x] 灵感结果限制为已知字段和合法类型，校验题材与子类；取消、离开模式或旧请求晚返回时不写入结果
- [x] 正文创作方式改为按书保存：AI 模式默认 AI 正文，其他入口默认人工主笔；每本书可单独切换（当时的全局默认回退已按第 24 节移除）
- [x] 创建流程中生成的大纲支持“编辑大纲 / 保存修改 / 取消”；编辑期间禁用确认分卷，确认后分卷与故事资料提取使用已保存的新大纲
- [x] 再次打开已有大纲时加载保存内容，不自动重生成；显式重新生成入口仍可使用
- [x] 写作辅助的“当前段落 / 当前场景 / 本章走向”置于滚动区外；分析中可切换范围并取消旧请求，防止旧结果覆盖新范围
- [x] 统一分卷解析，兼容加粗 Markdown、同行预估和原始字数；修复概要为 97 章、标题误显示 20 章的问题
- [x] 分卷预估章节数 / 万字数可编辑；旧分卷数字与概要不同可手动“从概要同步预估”，不静默覆盖作者数据
- [x] 自动化回归：36 个测试文件、98 项通过；类型检查、Knip 死代码审计与渲染器生产构建通过
- [x] 隔离浏览器回归：灵感对话 → 整理设定 → 手动修改 → 创建大纲 → 编辑大纲 → 确认分卷 → 人工主笔 → 写作辅助；仅使用合成小说和本地模拟接口
- [x] 分卷页面确认手动修订卷名保留，初始预估 97 章 / 21.3 万字；编辑后 98 章 / 22.1 万字正确保存并回显
- [x] 在 1280 × 720 和 390 × 844 窗口下验证辅助内容滚动到底，三个标签坐标不变且仍可切换；窄屏无页面横向溢出，最终检查控制台无警告或错误

联网与测试边界：

- 本节完成时，AI 对话 / 生成只请求已配置 API，尚未接入网页搜索工具。后续联网能力已在第 19 节实现；模型回复本身不代表已联网核实史实。
- 本轮真实接口尝试返回 HTTP 401“无效令牌”，真实模型端到端效果待有效配置后复验；以上浏览器成功流程来自明确标识的本地模拟响应，不等同于真实模型验收。
- 测试接口仅监听本机回环地址，不转发请求，不记录凭据；配置和合成数据使用独立测试 origin，不修改现有模型配置或书稿。
- 按作者最新要求，本轮不生成安装包或便携包，也不更新旧发布包；等待下次明确通知再打包。

## 19. 2026-09-05 对话联网与全项目审查

- [x] 右下角 AI 对话窗口增加「🌐 联网开 / 关」，默认关闭、按书保存，不影响正文/大纲/审查等其他 AI 功能
- [x] 联网支持 Responses 原生搜索、Claude Messages 原生搜索、OpenRouter web 插件、搜索专用 Chat Completions；模型设置增加协议选择
- [x] 只向作者配置的服务发送请求，不另行探测密钥、不失败后换域名/协议、不静默降级；对权限和不支持错误明确提示
- [x] 对话保存搜索状态和安全来源链接；无工具/引用证据时不宣称联网成功；关闭联网时阻止已知联网专用别名
- [x] 补充查询词外发、额外计费、模型仍需 API、服务商私有行为不能由客户端保证等说明
- [x] 请求绑定当前书籍，离开书籍/停止/卸载取消且拒绝晚到结果；不把接口失败信息再次发送给模型
- [x] 按作者补充要求：放大/还原/收起窗口不会停止任务，收起后继续后台运行并提示新回复；运行中切换联网只影响下一条
- [x] 移除无接收处理器的“应用建议”按钮，保留复制，正文不会被对话自动更改；清空记录增加确认
- [x] 创建新书的灵感模式复用联网协议和来源组件，使用大纲模型、独立开关，提供当前日期并区分近期事实与虚构；运行中改变开关只影响下一条
- [x] 灵感整理只使用已有对话、不重复搜索；安全来源和待核实说明有界带入可编辑设定，作者确认创建后随聊天记录保存
- [x] 常规测试 41 文件 / 132 项通过；类型检查、Knip、生产渲染构建和开发态 Electron 冒烟通过
- [x] 本地模拟接口浏览器回归：联网开关、搜索来源、400 不支持提示、后台继续执行、放大/收起和 390 × 844 窄屏
- [x] 灵感联网浏览器回归：联网构思 → 查看来源 → 关闭后普通聊天 → 整理并检查设定 → 创建测试书 → 重载后对话与来源保留；窄屏无溢出，重载后控制台无警告或错误
- [x] 新增 `docs/chat-web-search.md` 使用说明与 `docs/review-2026-09-05.md` 审查报告
- [x] 用独立检查复现知识库绑定未保存、立即保存提前返回、备份深层校验不足；浏览器复现公式值应为 7 但仍为 99
- [x] 后续优先修复已在第 20 节完成：公式解析与 CSP、知识库绑定保存、保存修订号确认、备份校验与导入回滚
- [x] 后续安全修复已在第 20 节完成：数据库损坏恢复、单实例锁、本地 Embedding CSP、两项中危依赖告警；大书稿保存仅完成本轮可测量部分，剩余项单独列出

本轮只实施书内/灵感联网对话及其直接相关生命周期修复，其余审查发现没有擅自修改。联网协议成功流程使用合成响应，真实中转权限/额度仍需实际接口验收。本轮不生成安装包或便携包。

## 20. 2026-09-05 审查问题修复与可靠性优化

作者授权「帮我优化」后实施，保留现有功能和书稿。详细记录及恢复指南见 `docs/optimization-2026-09-05.md`，原始审查报告保留历史证据并增加修复状态。

- [x] R1：受限公式解析器替代动态 `Function`，支持前向/链式引用，提示未知字段、循环依赖、重复字段、非数字与除零，不放松脚本 CSP
- [x] R2/R3：小说/知识库共享显式 dirty 与单调修订号保存器；绑定/解绑登记修改；立即保存等待目标修订落盘，关闭/导入排空期间新修改
- [x] R4：深层备份结构/体积/ID/关键引用校验，保留导入前恢复点，单事务导入小说/知识库/待确认 AI 正文草稿，失败回滚且 Store 不提前更新
- [x] R5：坏库进入只读恢复页；有效备份恢复或新建空库需确认，先保留原库副本；启动无需迁移时不重写数据库
- [x] R6：Electron 单实例锁，重复启动聚焦原窗口；Windows 原子替换失败保留旧文件
- [x] R7：CSP 定向允许本地 11434 回环服务，未全面开放 HTTP、未添加 `unsafe-eval`
- [x] R8：定向覆盖 xmldom 0.8.15 / fflate 0.8.3，全量/生产 `npm audit` 均 0 告警，锁文件一致性通过
- [x] 全局保存错误持久提示与重试；数据库/导入恢复说明同步记录
- [x] 移除两个 Store 的全量 deep watch，SQL 条件更新及有界行签名缓存减少重复写入
- [x] 百万字/1,500 个正文版本/5,000 条数据历史基准：单章修改实际写入由 5,502 行降到 2 行；未把波动的总耗时包装为性能提升
- [x] 知识文件大小、ZIP 条目/解压量、PDF 页数/文本量限制；DOCX 有界提取、EPUB spine 顺序和释放资源回归
- [x] TXT/Word「只导出正文」，Word 明确为 HTML `.doc`，修复 EPUB 非连续章节目录链接
- [x] 49 文件 / 166 项自动测试、类型检查、Knip、渲染器生产构建、开发态 Electron 冒烟通过
- [x] 隔离浏览器验证公式修复与失败提示、知识库绑定/解绑重载、项目备份导出、390 × 844 保存按钮可达与无横向溢出；控制台无警告/错误
- [ ] 后续性能：真实 Electron 大书稿磁盘基准、减少全书克隆与整库 export、正文按需加载
- [ ] 后续维护：继续拆分 ChapterEditor / CreateWizard，浏览器多标签写入保护与恢复点下载入口
- [ ] 待作者下次通知再制作便携包；现有旧发布包未更新，本轮不生成安装包

## 21. 2026-09-05 模型原始名称显示修复

- [x] 修复模型卡片和用途选择使用旧配置名称，导致不同模型都显示「Claude Sonnet 5」的问题，标题统一使用实际请求的原始模型 ID
- [x] 批量添加不再使用「旧配置名称 · 模型 ID」，每个新配置按自己的模型 ID 命名；保留重复配置跳过逻辑
- [x] 「配置名称」明确改为可选「配置备注」，单个保存留空时使用模型 ID；相同模型的多个接口可用备注区分
- [x] 现有配置备注、密钥、接口地址、配置 ID 和用途分配不做自动改写；模型 ID 编辑后标题同步更新
- [x] 增加 5 项设置页回归，覆盖旧错误名称、编辑后批量添加、空备注、同模型多接口和不改动旧配置
- [x] 全量 50 文件 / 171 项测试、类型检查、Knip 与渲染器生产构建通过；浏览器确认三张模型卡片及用途选择显示正确

本轮未请求真实中转接口、未改动已配置密钥，未生成安装包或便携包。

## 22. 2026-09-05 创建方式改为两组独立搭配

- [x] 第一组选择正文创作方式「AI 模式 / 写作辅助」，第二组选择设定准备方式「灵感模式 / 自选设定」，两组互不覆盖
- [x] 四种组合均可用；选好后明确点击继续，不因点击第一组选项直接离开选择页
- [x] 正文方式沿用全局默认值，选择只影响新书；灵感整理不能用返回数据覆盖作者已经选择的正文方式（全局默认已按第 24 节移除，新书初始选中写作辅助）
- [x] 灵感模式先对话、整理并检查可编辑设定，再进入大纲创建；自选设定保留手填、导入与大纲生成功能
- [x] 创建过程中显示当前搭配，可返回更换；保留已有表单和已确认的灵感记录，退出灵感界面后拒绝晚到整理结果
- [x] 选择界面抽为独立 `CreationModePicker.vue`，两组使用原生单选控件；窄窗口纵向排列，选择区域可滚动
- [x] 更新创建流程测试，覆盖四种组合落书、默认方式、独立选择、切换后保留数据、未激活时拒绝结果
- [x] 全量 50 文件 / 176 项测试、类型检查、Knip 与渲染器生产构建通过
- [x] 浏览器确认四种组合入口、当前搭配提示、灵感联网开关与自选设定页面；测试不发送真实 AI 请求、不额外创建书籍

未更改现有书籍的正文模式、密钥或用途分配，未生成安装包或便携包。

## 23. 2026-09-05 对话模型独立分配与来源名称展示

- [x] 设置「模型用途分配」增加「对话 / 联网模型」，默认动态跟随大纲模型；旧配置无需重新选择，删除已选对话模型后恢复默认
- [x] 灵感对话与右下角对话统一使用该模型；联网开关仅控制搜索，不切换模型；大纲生成与灵感设定整理继续使用大纲模型
- [x] 新分配独立持久化，不改写现有大纲/正文/审查分配、模型名称或密钥；请求使用发送时的模型快照
- [x] 聊天正文中已有来源的 Markdown 链接、自动链接和裸 URL 改为可读名称，兼容旧消息、转义与跟踪参数；未匹配的资料链接和作者输入保持原样
- [x] 来源区只显示可点击名称，URL 留在链接目标中；接口用 URL 当标题时显示「网页来源 N」，来源摘录也不堆砌长网址
- [x] 保留原始消息、来源 URL 和设定整理的证据数据；书内「复制」复制可读正文；继续保持 HTML 转义和安全链接校验
- [x] 全量 51 文件 / 194 项测试通过；类型检查、Knip 和渲染器生产构建通过
- [x] 隔离浏览器验证模型选择、灵感联网/普通对话和书内联网对话均使用专用模型；来源只显示名称且 href 保留；1280 × 720 页面无横向溢出
- [x] 真实设置页只读确认大纲/正文/审查原分配保持，新对话选项默认跟随大纲；689 × 698 窄窗口无页面横向溢出
- [x] 更新 `docs/chat-web-search.md`；模拟接口补充 Markdown 来源和模型标识用于后续回归

验证只调用本机合成接口，不请求真实 AI、不改动作者现有配置或书稿。修改前先备份未保存的灵感对话；开发热更新使创建页返回入口，该备份为本地恢复副本，不代表已自动恢复到聊天界面。本轮不生成安装包或便携包。

## 24. 2026-09-05 移除全局创作模式与灵感回车发送

- [x] 移除设置页「默认正文创作方式」及不再使用的控件导入
- [x] 按作者明确要求不保留旧版兼容：删除 Config 的全局 `writingMode` 字段、修改接口和编辑器全局回退；创作模式只从当前书籍读取
- [x] 新书初始选中写作辅助，可在创建时改选 AI 模式；Store 创建时显式保存该书模式，编辑器仍可按书切换
- [x] 创建页灵感输入框支持 Enter 发送、Shift+Enter 换行，并显示快捷键提示；保留发送按钮
- [x] 防止中文输入法选字、长按回车、空白消息、请求进行中和未激活页面的误发送或重复请求
- [x] 全量 51 文件 / 199 项测试通过，类型检查、Knip 与渲染器生产构建通过
- [x] 隔离浏览器确认设置页入口消失、创建页模式选择保留；真实按键确认 Shift+Enter 只换行、Enter 直接发送并收到本地模拟回复
- [x] 同步联网对话使用说明，并标注前期任务记录中已被替代的全局默认逻辑

本轮没有清空测试书籍、改动真实 API 配置或生成安装包；浏览器请求仅调用本机合成接口。未实施旧配置迁移或兼容层。

## 25. 2026-09-05 Windows 免安装发布包

- [x] 按作者本轮打包要求生成 Windows x64 免安装 ZIP，包含截至第 24 节的功能与清理结果
- [x] 51 个测试文件 / 199 项自动化测试通过；类型检查与 Vite 生产构建通过
- [x] Electron 打包后 EXE 冒烟通过：渲染正常、桥接可用、Node 隔离开启；本机系统密钥加密可用，使用独立临时数据目录
- [x] 检查 app.asar 无项目数据库、个人配置、恢复副本、对比仓库或 fixture 目录；打包应用代码未发现 Key 格式长字符串
- [x] 发现的两份依赖构建日志均逐字节匹配已安装的第三方依赖，不是作者本机运行日志
- [x] ZIP 内 128 个文件逐一解压读取并做 SHA-256 比对，全部匹配已测试的程序和使用说明；生成整包 SHA-256 校验文件
- [x] 保留旧 ZIP，本次使用带 20260905 日期的新文件名，避免混淆旧版本
- [x] 使用说明明确免安装不等于数据随目录迁移；跨电脑通过项目备份恢复书稿，另行配置 API；说明未签名及真实 API 未在本轮调用

产物：

- `release/AI-Novel-Writer-0.1.0-win-x64-portable-20260905.zip`：169,815,554 字节，约 161.9 MiB。
- `release/AI-Novel-Writer-0.1.0-win-x64-portable-20260905.zip.sha256`。
- SHA-256：`B80E222DA5854326372A88580D724658DADD2DFF9E841471A5EEEE58574826D7`。
- 使用说明源文件：`docs/portable-2026-09-05.md`，包内为 `使用说明.md`。

NSIS 安装包阶段因额外构建组件下载连接超时失败，未交付安装 EXE；已生成的 `win-unpacked` 不受影响，本次交付经过启动与压缩完整性验证的免安装 ZIP。没有上传外网、安装本软件或修改作者数据。

## 26. 2026-09-05 灵感对话阅读体验与关系图谱修复

- [x] 灵感对话区分作者与 AI：作者消息右对齐，AI 回复左对齐
- [x] AI 回复完成后定位到回复开头，流式输出过程中不再强制把用户拉到底部
- [x] 当前轮作者问题固定显示在对话区上方，支持展开长问题与回看原消息；新消息发送后自动替换
- [x] 大纲编辑栏改为独立不透明工具栏，正文单独滚动，编辑按钮不再呈现为被正文“镂空/遮挡”的状态
- [x] 角色扫描 JSON 增加人物关系字段；已有角色增加“补全关系”，支持角色资料、大纲与前 8 章正文节选，没有正文也可分析
- [x] AI 关系先预览确认，可修改、取消勾选后写入；角色编辑支持手动添加、修改和移除关系
- [x] 修复关系图与列表同时显示；两种视图互斥，图谱遵守搜索与状态筛选
- [x] 修复只画角色顺序正向关系的问题，ID 优先、名称回退匹配，单向关系也能连线；关系标签、空状态和数量可见
- [x] 点击图谱节点打开角色详情；详情包括传入关系，增加键盘可操作的角色入口
- [x] 图谱尺寸有界，窄屏图内滚动；主题切换及角色原位编辑后重绘
- [x] 关系数据写入角色 Store，重复扫描不会重复追加相同关系；停止、离页及资料变更后拒绝旧结果
- [x] 全量 52 文件 / 214 项测试通过，类型检查、Knip 和渲染器生产构建通过
- [x] 隔离浏览器验证合成关系预览、确认、连线与标签、点节点打开详情、刷新后保留关系；未调用真实 API
- [x] 浏览器完成“灵感对话 → 整理设定 → 生成大纲 → 编辑保存”；689×698、390×844 验证问题固定栏、展开、最新问题替换、回复开头定位、正文与工具栏分离；控制台无警告/错误
- [x] 修正本机回归脚本误认为创建向导有三步的问题；缩小问题展开高度，恢复输入区始终可达的严格断言

本轮没有修改作者现有书稿关系数据。已有角色需要点击“补全关系”并确认，或手动维护关系后显示连线。关系分析仅发送当前书籍资料给已配置的大纲模型，不联网搜索；资料歧义不自动定稿。浏览器测试仅在 5175 隔离来源使用合成资料，原开发环境 5173 已启动。旧便携包未更新，本轮未打包。

## 27. 2026-09-05 分离灵感对话与书内助手

- [x] 创建向导确认保留的灵感讨论写入独立 `inspirationHistory`，不再混入书内助手 `chatHistory`
- [x] 灵感记录持久化到 SQLite，已有测试书中带 `inspiration-<novelId>-<序号>` 前缀的旧记录安全迁移；普通书内对话不猜测、不移动
- [x] 工作台新增「灵感记录」页面：与书内助手独立回看、分页展示，作者消息与 AI 消息区分显示
- [x] 支持灵感记录导出为 Markdown / JSON；来源显示可读名称并保留安全可点击链接，不包含正文、总纲或书内助手消息
- [x] 清空书内助手对话不会删除灵感存档；助手上下文不再发送已否定的原始灵感，仅继续读取已确认书籍设定
- [x] 项目备份校验覆盖独立灵感记录；保留现有书籍、正文、模型和密钥配置不变
- [x] 新增分离、迁移、持久化、导出和界面回归测试；全量 54 文件 / 221 项测试通过，类型检查、渲染器构建和 Knip 通过
- [x] 当前 5173 浏览器验证刷新后灵感记录仍在、书内助手为空且显示独立提示、页面无横向溢出、控制台无警告/错误

本轮没有调用真实 AI，也没有删除作者任何聊天、正文或设定；旧便携包未更新。

## 28. 2026-09-06 写作辅助标签缓存与切换行为

- [x] 修复写作辅助切换标签会重复生成的问题；“当前段落 / 当前场景 / 本章走向”现在只切换视图，不会因切换自动调用 AI
- [x] 三个分析范围分别缓存自己的结果、加载状态、错误和选中方向；返回已分析标签直接显示原结果
- [x] 首次分析和“重新分析”仍由作者明确触发；正文、章节计划、人物、剧情规划、数据面板或知识库变化只标记结果可能过期，不自动重算
- [x] 切换标签不会取消正在进行的其他标签请求；停止、离开章节、切换书籍和组件卸载仍会取消并拒绝过期结果
- [x] 书内无正文时仍保留空状态，提示先选择范围再分析；当前 1390×1280 浏览器页面无横向溢出、控制台无警告/错误
- [x] 新增缓存隔离、重复请求、过期提示、资料变化和迟到结果回归测试；55 文件 / 238 项测试、类型检查、渲染器生产构建和 Knip 通过

本轮未调用真实 AI；当前浏览器已验证连续切换三个标签不进入加载状态，自动化测试断言切换不会调用分析服务。缓存限当前章节的编辑器会话，刷新或离开章节后需重新分析；旧便携包未更新。
